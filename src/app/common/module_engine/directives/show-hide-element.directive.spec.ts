import { ShowHideElementDirective } from './show-hide-element.directive';

describe('ShowHideElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHideElementDirective();
    expect(directive).toBeTruthy();
  });
});
