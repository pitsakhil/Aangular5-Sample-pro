import { DynamicIdDirective } from './dynamicid.directive';

describe('DynamicidDirective', () => {
    it('should create an instance', () => {
        // const directive = new DynamicIdDirective();
        // expect(directive).toBeTruthy();
    });
});
